<!DOCTYPE html>
<html lang="en">
{% include "_head.njk" %}

<body>
	<header>
		{%- include "_header.njk" -%}
	</header>

	<main class="page-margins">
		<div class="margin-fix">
			<div {%if page.url =='/' %} class="intro-wrapper pad-bottom" {% endif %}>
				<div class="secondary-header">
					{%if page.url != '/' %}
						<h1>{{ navtitle }}</h1>
					{% else %}
					<h1 class="cap-width-sm">Delightful things with pixels and words made by Francesco Imola from a seaside town in Kent.</h1>
					{% endif %}
					<div class="secondary-links">{% include "_links.njk" %}</div>
				</div>
				{%if page.url == '/' %}
				{% include "_intro.njk" %}
				{% endif %}
			</div>
			{{ content | safe }}
		</div>
	</main>

	{%- include "_footer-hello.njk" -%}

	{% if page.url == '/' %}
	<div id="rebrand-modal" class="modal">
		<div>
			<p>This site was formerly <b>nevernotready.club</b> and it is now <b>francescoimola.com</b></p>
			<p>Same work, same person, I'm just finally online under my own name.</p>
			<button onclick="closeModal()">Makes sense, lemme in</button>
		</div>
	</div>
	{% endif %}

	<script src="/assets/app.js" defer></script>
	{% if "/playground" not in page.url and "/colophon" not in page.url %}
	<script src="/assets/cal.js" defer></script>
	{% endif %}
</body>

</html>